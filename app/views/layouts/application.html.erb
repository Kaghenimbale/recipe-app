<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p class="notice class="color"><%= notice %></p>
    <p class="alert" class="color"><%= alert %></p>
    <%= yield %>
    <% if user_signed_in? %>
    <button class="sign-out fixed-links">
      <%= link_to("Sign Out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure'}) %>
    </button>
    <%= link_to "← Go Back", "javascript:history.back()", class: "go-back fixed-links" %>
    <% end %>
  </body>
</html>
